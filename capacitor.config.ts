import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lithosprotocol.aether',
  appName: 'The Lithos Protocol',
  webDir: 'dist'
};

export default config;
